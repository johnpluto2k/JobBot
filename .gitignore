.env
__pycache__/
*.pyc
.venv/
venv/

# Generated artifacts
data/
!data/.gitkeep

# Personal source documents (resumes, transcripts, cover letters, avatar)
documents/

# Raw source inputs — alumni sheet, job tracker (contains a password!), LinkedIn
# export. Sensitive PII/credentials; never commit.
inputs/

# OS / Office cruft
~$*
.DS_Store
.gstack/

# Claude Code personal settings
.claude/settings.json

# Dev-server run artifacts
*.log
*.pid
graphify-out/
screenshots/

# --- Personal source documents kept alongside the app (never committed) ------
# John's own resumes / cover letters and the Resume Studio scratch folder.
CoverLetter/
Resumes/
resume-studio/
Resume_Tailoring_Framework.md
