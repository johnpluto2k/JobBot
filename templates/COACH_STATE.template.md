# Coaching State — <YOUR NAME>

This is the running memory of the coaching conversation. Copy it to the repo
root as `COACH_STATE.md` (gitignored, never committed). The coach reads it after
`COACH.md` and before the live snapshot, so it never asks you to re-explain
settled context. It holds decisions, corrections, and open threads. It holds
**no live metrics**: counts, dates, and "is this posting still open" claims are
historical the moment they are written and must be re-verified from the
snapshot before being repeated.

> **Maintenance (for the coach):** after any substantive session, refresh the
> START HERE agenda and "Open threads", append one dated line to the Log, and
> keep the whole file under ~170 lines by compressing old log entries. Reread
> the file before saving; another session may have written newer decisions.

# ▶ START HERE — brief <first name> before anything else

When a session opens cold ("where are we", "what's next", or a question with no
context) pull the snapshot, then open with a short spoken-style summary of where
the search stands and these agenda items, in priority order:

1. <most time-sensitive open item>
2. <second item>
3. <third item>

End by asking which one to take first.

## Decisions made (don't relitigate)

- <YYYY-MM-DD> — <decision, and the one-line reason>.

## Corrections — established wrong, don't repeat

- <YYYY-MM-DD> — <the mistaken claim> → <what is actually true>.

## Operating rules

- <e.g. "Bullets-first: the coach delivers tailored bullets; I format the
  résumé myself in Overleaf.">
- <e.g. "Master résumé lives in Overleaf project <name>; it is the superset to
  trim from, not master_profile.json.">

## What's been built (so the coach doesn't suggest it again)

- <tool or workflow already in place>

## Open threads

- <company / thread> — <status and what is being waited on>

## Next actions

- [ ] <one concrete action, owner, by when>

## Log

- <YYYY-MM-DD> — <one line: what was discussed, what was decided>.
